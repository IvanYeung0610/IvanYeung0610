Ivan Yeung,  Andrew Piatetsky
SoftDev pd8
K32 -- screen saver
023-04-27th

DISCO:
    * Image() object allows us to port in image files
    * drawImage draws the image that is inputed in the parameter in the 2d context 
        * syntax: 
            drawImage(image, dx, dy)
            drawImage(image, dx, dy, dWidth, dHeight)
            drawImage(image, sx, sy, sWidth, sHeight, dx, dy, dWidth, dHeight)  

QCC: